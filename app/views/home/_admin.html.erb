<div class="col-sm-6">
  <table class="table table-bordered table-responsive">
    <thead>
      <tr>
        <th>Name</th>
        <th>Mobile</th>
        <th colspan="3"><%= link_to 'New Customer', new_customer_path, class: "btn btn-xs btn-info" %></th>
      </tr>
    </thead>

    <tbody>
      <% @customers.each do |customer| %>
        <tr>
          <td><%= customer.first_name + " " + customer.last_name %></td>
          <td><%= customer.mobile_num %></td>
          <td><%= link_to '', new_product_path(cust_id: customer.id), class:"btn btn-xs btn-info glyphicon glyphicon-plus" %></td>
          <td><%= link_to '', edit_customer_path(customer), class:"btn btn-xs btn-success glyphicon glyphicon-edit" %></td>
          <td><%= link_to '', new_payment_path(cust_id: customer.id), class:"btn btn-xs btn-warning glyphicon glyphicon-euro" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<div class="col-sm-6">
  <table class="table table-bordered">
    <thead>
      <tr>
        <th>Name</th>
        <th>Mobile</th>
        <th>Address</th>
        <th colspan="3"><%= link_to 'New Operator', new_operator_path, class: "btn btn-xs btn-info" %></th>
      </tr>
    </thead>

    <tbody>
      <% @operators.each do |operator| %>
        <tr>
          <td><%= operator.first_name + " " + operator.last_name %></td>
          <td><%= operator.mobile %></td>
          <td><%= operator.permanent_address %></td>
          <td><%= link_to '', record_operators_path(operator_id: operator.id), class:"btn btn-info btn-xs glyphicon glyphicon-th-list" %></td>
          <td><%= link_to '', edit_operator_path(operator), class:"btn btn-xs btn-success glyphicon glyphicon-edit" %></td>
          <td><%= link_to '', confirm_delete_operators_path(operator_id: operator.id), class:"btn btn-danger btn-xs glyphicon glyphicon-trash" %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>
